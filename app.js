const express = require('express');
const app = express();
const port = 3000;

// Rotas para /, /sobre, /seu_problema e /resposta_problema.

// Rota raiz
app.get('/', (req, res) => {
    
})
// Rota sobre
app.get('/sobre', (req, res) => {

})

// Rota seu problema
app.get('/sobre', (req, res) => {

})
